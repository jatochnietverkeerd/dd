import { useSafeToast as useToast } from "@/hooks/use-toast-safe"
import {
  Toast,
  ToastClose,
  ToastDescription,
  ToastProvider,
  ToastTitle,
  ToastViewport,
} from "@/components/ui/toast"

export function Toaster() {
  // Fallback toaster that doesn't break
  return null;
}
